<% if flash.any? %>
  <div class="space-y-2">
    <% flash.each do |type, message| %>
      <% next if message.blank? %>
      <div class="rounded-lg border border-slate-800 bg-slate-900/70 px-4 py-3 text-sm text-slate-100">
        <%= message %>
      </div>
    <% end %>
  </div>
<% end %>
